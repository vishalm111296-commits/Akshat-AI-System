# conftest.py — pytest configuration for Akshat-AI-System tests
import os
import sys

# Ensure repo root is on the Python path
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
