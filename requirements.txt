# Root requirements — single source of truth for development + CI
# Keep in sync with automation/requirements.txt
youtube-transcript-api>=0.6.0
feedparser>=6.0.0
pyyaml>=6.0
requests>=2.31.0
pytest>=7.0
